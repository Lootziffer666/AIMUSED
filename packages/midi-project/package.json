{
  "name": "@signal-app/midi-project",
  "version": "0.1.0",
  "description": "MUSE-derived MIDI import/export, project schema, local persistence and agent-command engine, ported to Signal",
  "type": "module",
  "scripts": {
    "test": "vitest run",
    "lint": "prettier --check src",
    "format": "prettier --write src"
  },
  "exports": {
    ".": {
      "default": "./src/index.ts",
      "types": "./src/index.ts"
    }
  },
  "devDependencies": {
    "fake-indexeddb": "^6.2.5",
    "typescript": "^5.9.3",
    "vitest": "^4.0.18"
  },
  "dependencies": {
    "@signal-app/orchestration-core": "*",
    "idb": "^8.0.3",
    "midi-file": "^1.2.4"
  }
}
